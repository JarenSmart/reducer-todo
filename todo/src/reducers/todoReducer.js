export const initialState = {
  item: "",
  completed: false,
  id: Date.now(),
};

//actions


//reducer
export const todoReducer = (state, action) => {
    switch() {
        default:
        return state;
    }
}